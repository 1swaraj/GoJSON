name: Setting up Github Actions
on:
  push:
    branches:
      - main
      - develop

jobs:
  notification:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Event
        strategy:
          matrix: ${{fromJson(github.event)}}
        run: |
          echo ${{ matrix.commits }}
