name: Setting up Github Actions
on:
  push:
    branches:
      - main
      - develop

jobs:
  generate-changelog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        run: |
          git log
